// Mui sx styles that will be used inside the sx attribute
  const styles = {
      // here sx style objects are defined
      MainBox:{
        display:'flex',
        justifyContent:'center',
        alignItems:'center',
        flexDirection:'column'
      },
      ExampleText:{
        fontSize:24,
        fontWeight:600,
        marginTop:2,
        marginBottom:2
      },
      ExampleButton:{
        marginTop:2,
        marginBottom:2
      }
  }
  
  export default styles